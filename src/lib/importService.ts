// src/lib/importService.ts
// Compatibility re-export for the canonical ingestion service.
// The real implementation now lives in src/lib/ingestion/importService.ts.

export * from "./ingestion/importService";
